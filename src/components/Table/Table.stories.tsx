import { Table } from "./Table";

const meta = {
  title: "Components/Table",
  component: Table,
};

export default meta;

export const Default = {};

export const Disabled = {
  args: {
    disabled: true,
  },
};